{
  "name": "chekssa",
  "private": true,
  "version": "0.1.1",
  "description": "Diffusion en live d'images annotées en overlay, par sessions.",
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build:client": "npm run build --workspace client"
  },
  "license": "MIT"
}
